apiVersion: score.dev/v1b1
metadata:
  name: "{{ cookiecutter.project_slug }}"
containers:
  "{{ cookiecutter.project_slug }}":
    image: .
    variables:
      SERVER_PORT: "8080"
service:
  ports:
    www:
      port: 8080
      targetPort: 8080
